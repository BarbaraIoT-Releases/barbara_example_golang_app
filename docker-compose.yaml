services:
  native_golang_builder:
    # Possible values: linux/amd64, linux/arm64, linux/arm/v7
    platform: linux/amd64
    container_name: native_golang_builder
    build: .
    volumes:
      - ./output:/output
